{
  "name": "stera-icons-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter stera-icons build",
    "build:icons": "pnpm --filter stera-icons build:icons",
    "dev": "pnpm --filter stera-icons dev",
    "dev:docs": "pnpm --filter docs dev",
    "test": "pnpm --filter stera-icons test",
    "lint": "pnpm --filter stera-icons lint",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
