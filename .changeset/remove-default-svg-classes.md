---
"stera-icons": patch
---

Remove default CSS classes from SVG icons. Icons no longer automatically include `stera` and `stera-[icon-name]` classes when rendered. Custom `className` props are still passed through as expected.
