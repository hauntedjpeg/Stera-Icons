---
"stera-icons": patch
---

Renamed main entry files from `stera-icons.js`/`stera-icons.d.ts` to `index.js`/`index.d.ts` for improved LLM and developer discoverability. Added a search hint comment at the top of the type definitions file and a "Finding Icons" section to the README explaining how to search `@tags` in `index.d.ts`.
